# Google Apps Script URL for RSVP form.
# Get this by deploying the Apps Script in your Google Sheet (source: apps-script/Code.gs).
# Used for BOTH the RSVP POST and the name-lookup GET (duplicate check + invite allow-list).
NEXT_PUBLIC_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
